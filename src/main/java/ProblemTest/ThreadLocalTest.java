package ProblemTest;

import Reference.P;

public class ThreadLocalTest {
    public static void main(String[] args) {
        ThreadLocal<P> personThreadLocal=new ThreadLocal<>();
       // personThreadLocal.set();
    }
}
